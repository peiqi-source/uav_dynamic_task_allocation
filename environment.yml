name: uav_task

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - pyyaml
  - tqdm
  - pytest
  - pip
  - pip:
      - tensorboard